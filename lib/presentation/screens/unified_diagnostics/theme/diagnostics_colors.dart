import 'package:flutter/material.dart';

/// Paleta de colores del técnico para pantallas de diagnóstico.
class DiagnosticsColors {
  DiagnosticsColors._();

  static const Color bg = Color(0xFF0a0e27);
  static const Color surface = Color(0xFF16213e);
  static const Color accent = Color(0xFF0f3460);
  static const Color cyan = Color(0xFF4dd0e1);
  static const Color green = Color(0xFF43A047);
  static const Color red = Color(0xFFE53935);
  static const Color amber = Color(0xFFFFB300);
  static const Color text = Colors.white;
  static const Color textDim = Color(0xFFb0bec5);
}
